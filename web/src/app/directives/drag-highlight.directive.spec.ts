import { DragHighlightDirective } from './drag-highlight.directive';

describe('DragHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new DragHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
