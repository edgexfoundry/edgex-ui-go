export interface ResourceProperties {
    valueType: string,
    readWrite: string,
    units: string,
    minimum: string,
    maximum: string,
    defaultValue: string,
    mask: string,
    shift: string,
    scale: string,
    offset: string,
    base: string,
    assertion: string,
    mediaType: string
}
